package com.mindcare.legacy.psychologue;

import com.mindcare.view.psychologue.*;

import com.mindcare.view.client.TicketsView;
import com.mindcare.utils.NavigationManager;
import javafx.scene.Node;

/**
 * PsychologueTicketsLegacyContent – same tickets UI as clients (reused with same mock data).
 */
public class PsychologueTicketsLegacyContent extends TicketsView {
    @Override
    public Node build() {
        return super.build();
    }
}
